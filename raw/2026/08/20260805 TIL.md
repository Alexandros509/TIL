
## 원본 필기 (Rough Notes)
교재 진도 : git&github
            - 1장 3강,5-6강 일부
            - 2장 1-4강
            - 3장 1-4강
            - 4장 2강 일부

어제의 복습  
git init 로컬 저장소 생성  
<div style="white-space: pre; font-family: Consolas, monospace; line-height: 1;">
 - working directory ------------> staging area / index -----------> repository  
                    git add                             git commit  
                                                        <-----------  
                                                        git reset --soft (커밋 취소)  
                    <-----------------------------------------------  
                                                        git reset --mixed (디폴트)  
 <------------------------------------------------------------------
                                                        git reset --hard

 </div>

 git reflog  
 git log  

 git add >  git status (확인) > git add -p (선택) > git diff --staged (커밋 전 확인)  

 add와 함께 커밋  
  git commit -a -m ____  
     -am ____  

  마지막 커밋 수정  
     git commit --ammend -m "메세지 컨벤션"

reset 복구시점까지만 기록이 남는다 (기록 유지 X)  
vs  
revert 복구 시점의 새로운 커밋을 생성 (기록은 유지)  

<div style="white-space: pre; font-family: monospace; line-height: 1.5;">
브랜치
 main / master - 완성본  
  생성 git branch branchname   feature/   
  이동 git checkout/switch branchname  
  생성+이동 git checkout -b bname  
              switch -c bname  
  삭제 git branch -d bname  
                  -D (d, D 다른 브랜치에서 삭제)   
  변경 git branch -m bn  
                -M bn (m, M 변경하고자 하는 브랜치로 이동 > 변경)  
&nbsp;
  병합 git merge bn
      1) Fast-Forward
         main 변경 사항 X. 서브 브랜치 라인 복사
      2) 3-way
        main sub 병합 > 새로운 커밋
&nbsp;
  재배치 git rebase bn
        커밋 기록 재배치. 깔끔하게 커밋 이력을 선형으로 정리하기 위해
            충돌 > 해결 (git add) > 진행 (git rebase --continue)
                   스킵 ( git rebase --skip) 현재 해결 건너뛰기 
                   중단 ( git rebase --abort) 전체 중단
&nbsp;
 push
 git push origin bn(원격브랜치)
        -u
 git remote add 원격저장소별칭 원격저장소주소
         set-url
 pull
 git pull origin bn
 

 Sourcetree 다운 및 사용법  

github compare & pull request 관련자 검토 승인 요청 느낌?  
    코드 히스토리 꼬여있으면 able to merge 안 뜸  
    내용 작성 
     예시) #작업 내용
...

#특이사항
...

#테스트 방법
...

[x] 항목1
[] 항목2

    confirm merge > delete branch (필요없어진 브랜치 삭제)
    main 브랜치 통합되면 x >  보호하기 위해 
        settings 들어가 rules > rulesets > new ruleset > new branch ruleset
        target branches의 add target > include by pattern (설명: pattern 문자열 ) > main > add inclusion pattern (확인)
        require a pull requset before merging 체크 
         - 선택 옵션 required approvals (안 하면 누구든지 올리고 얼마든지 수정)
         특정 팀 / 코드 주인 리뷰 요청, 소통 해결 필수로 할 수도 있음. 
         allow merge methods 중 squash (merge 전략. 커밋 여러개 하나로 합쳐서, 이력 깔끔 ) 
        active  

git push error 원격에 로컬에 없는 작업이 있어 발생.  
git pull --rebase origin main  

새 폴더 생성 (정해져있음)  
.github/pull_request_template.md  

git add . commit -m "docs: 풀리퀘스트 양식 추가" push origin main  

git switch -c feature/order-process  
문서 작성 add commit push 후 pull request 탭 create pull request > 브랜치 선택, 작성

projects 탭 > new project 탬플릿 kanban 
    project로 작업 흐름 파악 가능
    드래드 / ... 의 move to clumn으로 이동 가능
    item의 label로 상태 표시 가능
    milestone 데드라인 유의할점 목표 등 설정 
    create new issue 작업 (gitex#2)

문서 order 추가수정 add commit push 후 compare pull request 에
closes #2


git fetch  
 원격 저장소에서 가져옴 . pull = fetch + merge  

git pull --rebase 많이 씀  

issue 템플릿은 ai한테 많이 쓰는 초안으로 추천 예시 달라고.  

git stash  
git reflog : 복구  
git cherry-pick : 특정 커밋 추출접합. 너무 많이 사용하면 어지럽혀짐  

git tag  : 네이밍

git ignore : push 제외  

git rm --cached : 스테이징 명령 reset 외에도 이거 쓰면 가능  

git restore : 워킹 취소 명령  

git diff HEAD , <SHA1> <SHA2> : 비교 명령인데 --staged만 알아도 충분히 가능  

</div>

프로젝트 중 문제 발생
  - github main & branch 최신화 과정에서 push & pull 에서 잦은 충돌이 생김
    - git fetch 후 git switch
  - dev/map 브랜치에 내 로컬 컴퓨터에는 없는 새로운 커밋이 미리 존재하여 발생
    - git pull origin dev/map 후 git push origin dev/map


2번 문항
작업이 미완성 상태일 때 변경 사항을 임시로 스택에 저장하고 워킹 트리를 마지막 커밋 상태로 되돌리는 Git 명령어는 무엇인가?

rebort


오답이에요
git stash는 현재 작업 디렉토리와 스테이징 영역의 변경사항을 스택에 임시 저장하고 워킹 트리를 마지막 커밋 상태로 되돌립니다. 이후 git stash pop이나 apply로 복원할 수 있어, 긴급하게 다른 브랜치로 전환해야 할 때 유용합니다.


3번 문항
코드 리뷰에서 리뷰어가 'Request changes'를 선택했을 때 발생하는 상황으로 옳은 것은?

C. 단순 참고용 의견이므로 머지에는 아무 영향이 없다


오답이에요
Request changes는 버그, 보안 이슈, 설계 문제처럼 머지 전 반드시 수정이 필요한 경우 사용하는 액션입니다. Branch Protection Rules가 설정되어 있다면 리뷰어가 재확인 후 Approve하기 전까지 머지가 차단되어, 문제 있는 코드가 실수로 배포되는 것을 막아줍니다.

4번 문항
PR 본문에 'Closes #12'와 같이 특정 키워드와 Issue 번호를 함께 작성하면, PR이 머지될 때 자동으로 일어나는 동작은 무엇인가? (한 단어 또는 짧은 문장으로 답하시오)

병합


오답이에요
PR 본문에 Closes, Fixes, Resolves 등의 키워드와 Issue 번호를 함께 작성하면 PR이 머지되는 순간 해당 Issue가 자동으로 닫힙니다. 이를 통해 어떤 작업이 어떤 문제를 해결했는지 히스토리에서 추적할 수 있습니다.

5번 문항
GitHub Flow와 Git Flow를 비교한 설명 중 옳은 것은?

C. GitHub Flow는 브랜치 구조가 단순해 소규모·빠른 배포 주기의 팀에 적합하다

정답이에요
GitHub Flow는 main과 기능 브랜치만으로 동작하는 단순한 구조로, 소규모 팀이나 빠른 배포 주기에 적합합니다. 반면 Git Flow는 main, develop, feature, release, hotfix 등 복잡한 브랜치 구조를 가지며 중대형 팀의 버전 관리에 적합하고, 대형 팀의 짧은 주기 CI/CD 통합에는 Trunk-based Development가 더 적합합니다.

## 찾아본 내용 
md 공백 표시 `&nbsp;` , en space , em space  
사용자 코드 조각 (Snippets) -f1 > snippets > configure snippets > markdown > 입력  
   `<div style="white-space: pre; font-family: monospace;">`

알아 들여쓰기 shift + alt + f  

