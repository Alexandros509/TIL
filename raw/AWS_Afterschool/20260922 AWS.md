---
title:
date: "2026-09-22"
updated: "2026-09-22"
category:
tags:
  -
---


도커 Docker

백엔드 직접 배포?
fastapi 
백엔드 배포에 도커 많이 씀

도커 역할
 1. 서버 
 2. 운영체제 
    -  서비스하는 게 운영체제랑 개발환경 똑같을까? nope 맞출 수 없음
    -  그럴 때 윈도우가 아닌 가상환경 개발 virtual machine 안에서 개발/테스트
    -  그렇게 하는 거 말고도 간단하게 설정 >> Docker
	    -  내가 원하는 환경과 일치하는 환경 설정 가능
	    -  아키텍쳐 설정 가능
	    -  cpu lam 운영체제 가능
	    -  특정버전 
	    -  파이썬 버전 지정
	    -  requirement.txt , pip install
	    -  도커는 다 자동
	    -  사용할 라이브러리 일괄 호출
	    -  최신버전 X 가장 안전한/ 개발환경과 똑같은 버전으로
	    -  dockerfile로 지정
	    -  main.py 엔드 포인트 넣음
		    -  FROM python:3.12-slim
				WORKDIR /app
				COPY requirements.txt .
				RUN pip install --no-cache-dir -r requirements.txt && useradd --uid 10001 --create-home app && mkdir /data && chown app:app /data
				ARG SERVICE
				COPY ${SERVICE}/main.py /app/main.py
				USER app
				ENV PYTHONUNBUFFERED=1 PYTHONDONTWRITEBYTECODE=1
				CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
	    -  하나의 main.py에 몰려있다 하나 뻑나면 다 무너짐
	    -  각각의 서비스 나누고 안정적 상태 
	    -  각각 서비스 하나하나 호출 귀찮 번거롭 > 도커
	    -  각각의 포트 분배 
	    -  해당 컴퓨터 서버로 누구나 8001에 들어올 수 있음 > 위험
		    -  8001 포트로 db 접근 위험
		    -  sql injection
			    -  id :  ,password: 패스워드에 특정 sql 문법 기입 `* FROM admin ~~~` 
				-  admin id pw에 적는 거 방지 등
				-  공개할 포트와 은닉 포트 분류
		-  백엔드 구조 설계
			- api 정의서
			- api  post get put delete
			-  restful api 규칙
			-  crud
			-  백엔드 주소 다음 api 이름(엔드포인트) 정의 - 개발자가 1번째 할 일
			-  api python fast api 활용 - 자동, swagger ui 기본
			-  옛날에는 파이썬 느렸지만 지금은 매우 유사 비슷해서 python fast api 넘어가는 추세
			-  git에 올리고 dockerfile 올리면 끝
		-  ec2 생성
			-  대충 만들고
			-  aws 보안 기능 이용해서 보안 사고 방지
			-  컴퓨터 1대에 여러 가상환경에 인바운드 풀어줘야하는 거 아님?
				-  aws 내에서 같은 서버 내에서는 괜찮.
				-  aws 서버 우분투에 도커 설치
				-  git 연결
				-  위치는 항상 compose.yml 파일 있는 위치에서 실행 
				-  sudo docker compose -up -build
				-  sudo docker compose ps
				-  이 상태로 못 들어감.  포트 로컬 호스트에서만 입장 가능
				-  포트 0.0.0.0 으로 바꿔줘야함
				-  퍼블릭 들어가서 
				-  포트 번호 숨기기 - 도메인 쓰면 끝남.
				-  부모 자식 도메인 
				-  naver.com 가 도메인. www/api/store/ 등등 앞에 다 도메인 구매가 아님
				-  뒤가 메인이고 앞은 하위
				-  나중에 route 53에서 설정
				-  body api 명세서 대로 


		☆★☆ 나중에 튜터님 블로그 링크 내용 첨부하자
			  월수 수업 긴 버전으로 들어 보충해야하나?


과제가 백엔드 서버 만들어보기. ai 활용. 로그인은 카카오 developer에서. 플랫폼 키 api 키. 
 조건 : 
	 아래는 ai를 통해 만들 때 필요한 조건입니다.  
		1. python 3.10이상
		2. FastAPI를 사용할 것
		3. Docker로 구현할 것
		4. MSA방식으로 구현할 것

토스페이먼츠에 결제 api도 가능하고 테스트 키 줌.
포트원 원하는 서비스 연결 가능.


readme에 상세 내용 작성 후 클로드 주면 됨. 그게 요즘 추세.