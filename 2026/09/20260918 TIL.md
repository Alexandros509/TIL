---
title: 뷰포트·미디어 쿼리·브레이크포인트
date: 2026-09-18
category: Frontend
tags:
  - Viewport
  - Media Query
  - Breakpoint
---

# 오늘의 TIL

> 날짜 : 2026-09-18

> 주제 : 뷰포트·미디어 쿼리·브레이크포인트

[원본 노트](../../raw/2026/09/20260918%20TIL.md)

## 어제의 복습

1. CSS 구조(HTML)와 디자인(CSS)의 완전한 분리로 재사용성 및 유지보수성을 극대화할 수 있는 방식
   - 외부 스타일 시트
2. 한 요소에 여러 CSS 규칙이 충돌할 때, 선택자가 더 구체적이고 좁은 범위 타켓팅할수록 점수가 높게 산정되어 최우선 적용되는 규칙
   - 명시도
3. 부모 요소 font-size: 16px , 자식 요소 line-height: 1.5를 지정했을 때 실무에서 이 방식(단위 없는 숫자)을 권장하는 이유
   - 계산된 고정 픽셀값이 아닌 '비율' 자체가 상속되기 때문
4. box-sizing: border-box;가 적용된 요소의 width: 200px; padding: 20px; border: 5px solid red; 이 요소가 실제로 차지하는 가로 폭
   - 250px
5. Flexbox 자식 요소가 부모 공간의 남는 여백을 나누어 가지도록 확장 비율을 지정할 때 사용하는 속성명
   - flex-grow

## 오늘의 학습

### 뷰포트 (Viewport)

보이는 화면

~~~css
<meta name="viewport" content="width=device-width,initial-scale=1,minimum-scale=1,maximum-scale=1,user-scalable=no,interactive-widget=resizes-content">
~~~

| 속성 | 값 | 메모 |
|------|-----|------|
| `width` | `device-width` / `device-height` | 기기 폭·높이 |
| `user-scalable` | `yes` / `no` | 확대·축소 |
| `initial-scale` | 숫자 | 처음 비율 |
| `minimum-scale` / `maximum-scale` | 숫자 | 축소·확대 한계 |

### 브레이크포인트

- 레이아웃이 바뀌는 기준 해상도 → CSS 적용 분기
  - 모바일 (~767px)
  - 태블릿 (768px ~ 1023px)
  - 데스크탑 (1024px~)

### 미디어 쿼리 `@media`

- 문법: `@media 미디어타입 and (미디어특성) {}`

**미디어타입**

- `all` (default) / `screen` / `print` / `speech`

**미디어특성**

- `width` / `height`
- `min-width` (≥) / `max-width` (≤)
- `orientation`: `portrait`(세로) / `landscape`(가로)

## 핵심 개념

- Viewport meta: `width=device-width` 등
- 브레이크포인트: ~767 / 768~1023 / 1024~
- `@media 타입 and (특성) {}`
- 타입: all·screen·print·speech / 특성: min·max-width, orientation

## 교재 보충

※ 아래 내용은 사용자의 필기를 보완하기 위해
교재에서 가져온 내용이다.

### 미디어 쿼리 (FE 3-2)

기기 특성(화면 타입·가로 너비 등)을 감지해, 조건에 맞을 때만 CSS를 적용.

~~~css
@media screen and (min-width: 768px) {
  body { background-color: lightgreen; }
}
~~~

| 디바이스 | 경계 |
|----------|------|
| 모바일 | `max-width: 767px` |
| 태블릿 | `min-width: 768px` and `max-width: 1023px` |
| 데스크톱 | `min-width: 1024px` |

## 필기 ↔ 교재 비교

| 항목 | 필기 | 교재 | 결과 |
|------|------|------|------|
| 뷰포트 | meta + device-width | 동일 | 동일 |
| 브레이크포인트 | ~767 / 768~1023 / 1024~ | max 767 / min 768·max 1023 / min 1024 | 동일 |
| `@media` | 타입·특성 나열 | 정의 + 예시 | 교재 보충 |
| Tailwind | 없음 | CDN·유틸·`md:` | 필기 누락 가능성만 |

## 확인이 필요한 사항

⚠ 복습 4번 답 `250px`. border-box면 width 200에 padding·border가 포함 → 가로 폭은 보통 `200px`. 필기 답 유지. (200+20×2+5×2=250 vs border-box 200)

⚠ viewport 샘플 fence가 `~~~css`인데 내용은 HTML `<meta>`. 필기 표기 유지. `html`로 바꿀지 확인.

## 필기 누락 가능성

다음 내용은 교재에는 있으나
오늘 수업에서 다루었는지는 확실하지 않습니다.

- FE 3-2: Tailwind CSS (CDN, 유틸리티, 반응형 접두사 `md:` 등)

※ 수업에서 다루었다면
필기를 놓쳤을 가능성이 있습니다.

## 편집 로그

### 수정한 내용

- 흐름: viewport meta → breakpoints → `@media` 문법 → 타입 → 특성
- meta 속성 소표 추가, 본문 압축
- 확인 항 문장 단축 (border-box 250px, css fence vs HTML meta)
- Tailwind는 필기 누락만. scope FE 3-2 유지
- Front Matter·원본 링크 유지

### 수정하지 않은 내용

- 복습 4번 `250px`
- viewport fence `~~~css`·meta 원문
- Tailwind를 오늘의 학습에 넣지 않음

### 추가 정보 출처

- 필기
- 교재 (FE 3-2)
