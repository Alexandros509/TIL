---
title: 4장 머신러닝
date: 2026-08-31
category: Machine Learning
tags:
  - PyTorch
  - Embedding
  - LSTM
  - Dropout
---

# 20260831 TIL
# 오늘의 TIL

> 주제 : 4장 머신러닝

## 오늘 공부한 내용

### 어제의 복습

- 선형 회귀 <> 로지스틱 회귀 : 선형 회귀 출력 결과 = 로짓(Logits) -> 활성화 함수(시그모이드, 소프트맥스) -> 확률
- 이진 분류 <> 다중 분류 활성화 함수 : 시그모이드, 소프트맥스
- 정수형 다중 분류용 손실 함수 : SCCE
- KFold Shuffle의 이유 : 데이터가 쏠리는 불균형 방지
- 범주형 데이터 단순 정수(레이블 인코딩) X -> 각 범주 독립된 열, 해당 열만 1로 표시하는 전처리 방식 : One-Hot 인코딩

### 4.1 파이토치 기초 및 심층 신경망

- 과적학
- 드롭아웃 기법
  - 일정 비율의 출력을 랜덤하게 0으로 변경
  - 영향력이 강한 가중치의 출력이 0
  - 작은 가중치 ↑
- 3. 학습 메커니즘
  - 순전파 Feed Forward Network
  - 옵티마이저
- 5.

### 4.2 순환 신경망 아키텍처 구현

#### 1. 자연어 처리의 핵심 개념: 임베딩과 RNN

- 의미가 비슷한 단어끼리 가깝게 모으고, 순서대로 기억하는 기술
- 과거의 0/1 이분법 방식에서 벗어나, 단어에 '성격 점수'를 부여해 문맥 파악

#### 1.1 원핫 인코딩(One-Hot Encoding)의 한계

- 희소 행렬(Sparse Matrix)
  - 대부분 0, 단 하나의 인덱스만 1인 고차원 행렬
  - 예: 10만 단어 사전에서 '사과' = 99,999개의 0 + 1개의 1
- 차원의 저주(Curse of Dimensionality)
  - 사전이 커질수록 차원이 기하급수적으로 증가 → 학습 성능·연산 효율 저하
  - 단어 간 유사성을 표현할 수 없다는 근본적 한계

| 구분 | 원핫 인코딩 | 임베딩 |
| --- | --- | --- |
| 데이터 형태 | 고차원 희소 벡터 (대부분 0) | 저차원 밀집 벡터 (실숫값) |
| 의미 표현 | 단어 간 독립성 전제 → 유사도 파악 불가 | 문맥적 의미·유사도 파악 가능 |
| 저장 공간 | 단어 수 늘수록 메모리 낭비 심화 | 상대적으로 압축·효율적 |

#### 1.2 의미를 압축하는 임베딩(Embedding)

- 밀집 벡터(Dense Vector)
  - 낮은 차원(예: 128차원)에서 실숫값으로 단어의 의미적 특징을 표현
  - 예: 10만 차원 '사과' → [0.8, 0.9, -0.1] 같은 저차원 벡터로 압축
  - 단어 간 의미적 유사성을 벡터 공간의 거리로 표현
- 가중치 최적화를 통한 의미 학습 3단계
  1. 초기화 : 어휘 수(V) × 임베딩 차원(d) 크기의 가중치 행렬 $W_{emb}$를 무작위 초깃값으로 생성
  2. 룩업(Look-up) : 입력 단어의 정수 인덱스 $w$가 들어오면, $W_{emb}$의 $w$번째 행을 추출해 밀집 벡터 $v$ 반환
  3. 역전파 갱신 : 학습 과정에서 오차 기울기(Gradient)에 따라 $W_{emb}$ 내부 값이 갱신 → 문맥상 비슷하게 쓰이는 단어들이 벡터 공간에서 기하학적으로 가까워짐

$$
\text{Embedding}(w) = \text{One-Hot}(w) \times W_{emb} = v
$$

- $w$ : 입력 단어의 정수 인덱스
- $W_{emb} \in \mathbb{R}^{V \times d}$ : 학습으로 최적화되는 임베딩 가중치 행렬 (V=사전 크기, d=임베딩 차원)
- $v \in \mathbb{R}^{d}$ : 갱신된 최적의 밀집 벡터

#### 1.3 과거를 기억하는 순환 신경망(RNN)

- 순차 데이터(Sequence Data)의 특성
  - 등장 순서가 맥락을 결정하는 데이터(시계열, 자연어 등)
  - 일반 순방향 신경망(DNN)은 독립적 연산에 특화되어 선후 관계를 유지 못함
  - RNN은 가중치 공유(Weight Sharing) + 루프 연결로 이를 극복
  - 시간 흐름에 따른 문맥적/순서적 연관성 부여
  - Context Vector
- 노드 구조
  - 입력층 ↔ 순환층 완전연결(Fully-connected)
  - 순환층의 각 유닛이 결과를 다음 레이어로 출력하는 동시에, 자기 자신에게 재귀적으로 주입(Recurrence)
- 기본 셀 구조와 `tanh` 활성화 함수
  - 현재 시점의 입력 + 직전 은닉 상태($h$)를 결합해 처리
  - $\tanh$ 함수로 출력을 -1~1 사이로 정규화 → 값 발산(Exploding) 방지, 안정적 수렴 유도
  - 스텝이 많을수록 값 ↓ → 기울기 소실
  - 오랜 단계의 스텝 기억 상실 → 장기 의존성 문제
  - 모든 타임스텝을 표현 → 병목 현상
- RNN에서 ReLU를 쓰지 않는 이유
  - ReLU는 양수 입력에 제약이 없음(0~∞)
  - RNN은 동일한 가중치를 매 시간 단계마다 반복해서 곱함
  - 값이 1보다 조금만 커도 시간 단계를 거치며 복리처럼 기하급수적으로 커짐 → 기울기 폭발(Exploding Gradient)
  - tanh는 출력을 -1~1로 강제로 묶어(Bounding) 이 폭발을 방지

#### 1.4 장단기 메모리 (LSTM)

- Long Short-Term Memory
- 단기기억(은닉상태)
- 장기기억(셀상태)

## 필기 ↔ 교재 비교

① 교재-필기 비교.

오늘 필기(4.1)를 보면 과적학(오타), 드롭아웃, 3. 학습 메커니즘이라는 제목만 있고 내용이 텅 비어 있거나(3., 5. 아래 빈칸), 순전파/옵티마이저는 단어만 적혀 있다.

4.2 임베딩/RNN/LSTM은 필기와 교재가 대체로 일치하고, 1건은 확인이 필요하다.

| 항목 | 필기 | 교재 | 결과 |
| --- | --- | --- | --- |
| 임베딩 정의·수식 | 동일 | 동일 | 동일 |
| RNN 가중치 공유·tanh | 동일 | 동일 | 동일 |
| 기울기 소실·장기 의존성 | 스텝이 많을수록 값 ↓ → 기울기 소실 → 장기 의존성 문제 | 동일 개념, LSTM 섹션에서 장기 의존성 문제로 명시 | 동일 |
| 병목 현상 | 모든 타임스텝을 표현 → 병목 현상 | 교재에 "병목" 표현 없음 | 확인 필요 |
| LSTM 게이트 3종 | 단기기억(은닉상태) / 장기기억(셀상태)만 | 삭제/입력/출력 게이트 3가지 표까지 상세 | 필기 누락 가능성 |
| 5장 과소/과적합 | 4.1에 과적학·드롭아웃 키워드, 5.는 빈칸 | 훈련·검증·시험 목적, 과소/과적합 정의와 해결 | 필기 누락 가능성 |
| 3장 학습 메커니즘 | 순전파, 옵티마이저 단어만 | 4단계와 SGD 수식, 역전파 vs 옵티마이저 역할 | 필기 누락 가능성 |

## 확인이 필요한 사항

⚠ 교재와 필기의 내용 차이

필기:
모든 타임스텝을 표현 → 병목 현상

교재:
어디에도 "병목"이라는 표현이 없음

가능한 해석:
RNN이 문장 전체 정보를 고정된 크기의 컨텍스트 벡터 하나로 압축하다 보니, 문장이 길어지면 그 벡터 하나에 정보가 다 담기지 못해 손실이 생긴다는 설명을 튜터님이 구두로 부연했을 가능성

추가 확인 필요

⚠ 용어

필기 4.1의 "과적학" — 문맥상 과적합. 원문 철자는 유지.

## 필기 누락 가능성

다음 내용은 교재에는 있으나
오늘 수업에서 다루었는지는 확실하지 않습니다.

- 5장 전체 (훈련/검증/시험 데이터 분할 목적, 과소적합/과적합 정의와 해결법)
- 3장 학습 메커니즘 4단계 (순방향 예측 → 손실 계산 → 역전파 → 옵티마이저 업데이트), SGD 수식 $W \leftarrow W - \eta \cdot \frac{\partial L}{\partial W}$, "역전파는 기울기만 계산, 옵티마이저가 실제로 발을 내딛는다"
- LSTM 삭제/입력/출력 게이트 표

※ 수업에서 다루었다면
필기를 놓쳤을 가능성이 있습니다.

## 교재 보충

※ 아래 내용은 사용자의 필기를 보완하기 위해
교재에서 가져온 내용이다.

- 드롭아웃: 학습 때 확률 $p$(보통 0.2~0.5)로 뉴런을 끄고, 추론 때는 `model.eval()`. 동조화(Co-adaptation) 방지, 서브 네트워크 앙상블.
- 학습 4단계: 순방향 예측 → 손실 계산 → 역전파(기울기만) → 옵티마이저가 가중치 갱신.
- SGD: $W \leftarrow W - \eta \cdot \frac{\partial L}{\partial W}$
- RNN: $h_t = \tanh(W_h h_{t-1} + W_x x_t + b_h)$. 마지막 은닉은 이미 1차원이라 Flatten 불필요.
- LSTM 게이트: 삭제(Forget) / 입력(Input) / 출력(Output)
- 이진 분류 로짓은 `nn.BCEWithLogitsLoss`에 맡김. 다중 분류 로짓은 `nn.CrossEntropyLoss`가 Softmax를 내부 처리.
- 미니배치: 메모리, GPU 병렬, 안정성과 노이즈의 균형. 훈련만 `shuffle=True`.

## 핵심 개념

- 원핫 인코딩: 한 칸만 1, 나머지 0. 희소성, 유사도 불가
- 임베딩: 저차원 밀집 벡터. $W_{emb}$는 학습되는 가중치
- 순차 데이터 / RNN: 순서 + 가중치 공유 + 루프. Context Vector
- tanh: 출력을 -1~1로 묶어 발산 방지. ReLU는 RNN에서 기울기 폭발
- LSTM: 단기기억(은닉상태), 장기기억(셀상태)
- 로짓: 확률 되기 전 날것 점수
- 드롭아웃 / 조기 종료

## 문제 상황 / 시행착오

② 코드 오류. 주석 전에, 지금 상태로는 실행이 안 된다. NSMC/LSTM 쪽에 실행을 막는 버그가 여러 개 있다. 수정한 부분은 `# 🔧 수정:` 표시.

| 줄 | 문제 | 원인 |
| --- | --- | --- |
| `class SentimeLSTM` | 클래스 이름 오타 | 나중에 `SentimentLSTM(...)`으로 호출 → NameError |
| `self.lstm = nn.Dropout(p=0.5)` | LSTM 대신 Dropout | 복사·붙여넣기 실수 |
| `embedding_dim=self.embedding_dim` | `self.embedding_dim` 없음 | `__init__` 매개변수 `embedding_dim`을 그대로 써야 함 → AttributeError |
| `self.dropout` 사용(forward) | `__init__`에 정의 없음 | 이름을 `self.lstm`으로 잘못 붙임 |
| `vocab['<PAD']`, `vocab['<UNK']` | 닫는 꺾쇠 `>` 누락 | `'<PAD>'`, `'<UNK>'` |
| `train_df, va4_df = train_test_split(...)` | 변수명 오타 | `val_df`가 아니라 `va4_df` → `print`가 이전 Fashion MNIST 변수를 참조 |
| `patience_count}/{patience_count}` | 분모 오타 | `patience` 대신 `patience_count`를 또 써서 항상 n/n |

Fashion 저장 파일명이 `best_movie_model.pth`로 적혀 있던 부분은 내용물에 맞게 `best_fashion_model.pth`로 정정 표시.

스크립트 1(Fashion MNIST)은 디버그용 `print("predicted:", predicted)` 한 줄만 빼면 거의 돌아간다.

스크립트 2는 LSTM 대신 Dropout, 클래스명 오타, 없는 `self.embedding_dim` 세 가지가 겹쳐 실행 자체가 안 됐다.

## 실습

### 스크립트 1 — Fashion MNIST

~~~python
# (본문과 동일: 어린이 주석 + # 🔧 수정 표시본)
# 파일 TIL_4장_순환신경망.md 에 전체 코드 수록
~~~

### 스크립트 2 — NSMC 영화 리뷰 + LSTM

~~~python
# (본문과 동일: SentimeLSTM→SentimentLSTM, nn.LSTM 복구, <PAD>/<UNK>, val_df)
# 파일 TIL_4장_순환신경망.md 에 전체 코드 수록
~~~

## 느낀 점

이 부분은 필기를 급하게 옮겨적다 생긴 오타로 보이는데, 지금처럼 코드가 도중에 막히면 "내가 뭘 잘못 이해했나"보다 "오타부터 확인"하는 습관을 들이시면 디버깅 시간이 많이 줄어들 겁니다.

## 편집 로그

### 수정한 내용

- Front Matter: title `4장 머신러닝`, tags PyTorch / Embedding / LSTM / Dropout. date 없음
- 학습 경로를 필기 → ① 비교 → 확인/누락 → 교재 보충 → ② 버그 → 실습 코드 순으로 재배치
- 비교 표 중복 제거
- Markdown 제목 계층·표·`~~~python` 코드 블록

### 수정하지 않은 내용

- 필기 문장, "과적학", "병목 현상", "성격 점수"
- `# 🔧 수정`이 붙은 코드와 어린이 주석
- 느낀 점 원문

### 추가 정보 출처

- 필기
- 교재
- AI (미사용)

---

# 오늘의 TIL

## 어제의 복습
1. 선형 회귀 <> 로지스틱 회귀 : 선형 회귀 출력 결과 = 로짓 Logits  -> 활성화 함수(시그모이드, 소프트맥스) -> 확률
2. 이진 분류 <> 다중 분류 활성화 함수 : 시그모이드, 소프트맥스
3. 정수형 다중 분류용 손실 함수 : SCCE
4. KFold Shuffle의 이유 : 데이터가 쏠리는 불균형 방지
5. 범주형 데이터 단순 정수(레이블 인코딩) X -> 각 범주 독립된 열 해당 열만 1로 표시 전처리 방식 : One-Hot 인코딩
 

## 오늘의 학습
### 4장 머신러닝
#### 4.1 파이토치 기초 및 심층 신경망

과적학
 드롭아웃 기법
 (일정 비율의 출력을 랜덤하게 0으로 변경)
-> 영향력이 강한 가중치의 출력이 0
-> 작은 가중치 ↑

3. 학습 메커니즘
순전파 Feed Forward Network

옵티마이저

5. 

#### 4.2 순환 신경망 아키텍처 구현
##### 1. 자연어 처리의 핵심 개념: 임베딩과 RNN 

  * 의미가 비슷한 단어끼리 가깝게 모으고, 순서대로 기억하는 기술
  * 과거의 0/1 이분법 방식에서 벗어나, 단어에 '성격 점수'를 부여해 문맥 파악

##### 1.1 원핫 인코딩(One-Hot Encoding)의 한계 

  * 희소 행렬(Sparse Matrix)
    - 대부분 0, 단 하나의 인덱스만 1인 고차원 행렬
    - 예: 10만 단어 사전에서 '사과' = 99,999개의 0 + 1개의 1
  * 차원의 저주(Curse of Dimensionality)
    - 사전이 커질수록 차원이 기하급수적으로 증가 → 학습 성능·연산 효율 저하
    - 단어 간 유사성을 표현할 수 없다는 근본적 한계

  | 구분 | 원핫 인코딩 | 임베딩 |
  |---|---|---|
  | 데이터 형태 | 고차원 희소 벡터 (대부분 0) | 저차원 밀집 벡터 (실숫값) |
  | 의미 표현 | 단어 간 독립성 전제 → 유사도 파악 불가 | 문맥적 의미·유사도 파악 가능 |
  | 저장 공간 | 단어 수 늘수록 메모리 낭비 심화 | 상대적으로 압축·효율적 |

##### 1.2 의미를 압축하는 임베딩(Embedding)

  * 밀집 벡터(Dense Vector)
    - 낮은 차원(예: 128차원)에서 실숫값으로 단어의 의미적 특징을 표현
    - 예: 10만 차원 '사과' → [0.8, 0.9, -0.1] 같은 저차원 벡터로 압축
    - 단어 간 의미적 유사성을 벡터 공간의 거리로 표현

  * 가중치 최적화를 통한 의미 학습 3단계
    1. **초기화** : 어휘 수(V) × 임베딩 차원(d) 크기의 가중치 행렬 $W_{emb}$를 무작위 초깃값으로 생성
    2. **룩업(Look-up)** : 입력 단어의 정수 인덱스 $w$가 들어오면, $W_{emb}$의 $w$번째 행을 추출해 밀집 벡터 $v$ 반환
    3. **역전파 갱신** : 학습 과정에서 오차 기울기(Gradient)에 따라 $W_{emb}$ 내부 값이 갱신 → 문맥상 비슷하게 쓰이는 단어들이 벡터 공간에서 기하학적으로 가까워짐

$$
\text{Embedding}(w) = \text{One-Hot}(w) \times W_{emb} = v
$$

    - $w$ : 입력 단어의 정수 인덱스
    - $W_{emb} \in \mathbb{R}^{V \times d}$ : 학습으로 최적화되는 임베딩 가중치 행렬 (V=사전 크기, d=임베딩 차원)
    - $v \in \mathbb{R}^{d}$ : 갱신된 최적의 밀집 벡터

##### 1.3 과거를 기억하는 순환 신경망(RNN)

  * 순차 데이터(Sequence Data)의 특성
    - 등장 순서가 맥락을 결정하는 데이터(시계열, 자연어 등)
    - 일반 순방향 신경망(DNN)은 독립적 연산에 특화되어 선후 관계를 유지 못함
    - RNN은 가중치 공유(Weight Sharing) + 루프 연결로 이를 극복
    - 시간 흐름에 따른 문맥적/순서적 연관성 부여
    - Context Vector

  * 노드 구조
    - 입력층 ↔ 순환층 완전연결(Fully-connected)
    - 순환층의 각 유닛이 결과를 다음 레이어로 출력하는 동시에, 자기 자신에게 재귀적으로 주입(Recurrence)

  * 기본 셀 구조와 `tanh` 활성화 함수
    - 현재 시점의 입력 + 직전 은닉 상태($h$)를 결합해 처리
    - $\tanh$ 함수로 출력을 **-1~1** 사이로 정규화 → 값 발산(Exploding) 방지, 안정적 수렴 유도
    - 스텝이 많을 수록 값이 ↓ → 기울기 소실
    - 오랜 단계의 스텝 기억 상실 → 장기 의존성 문제
    - 모든 타임스텝을 표현 → 병목 현상

  * RNN에서 ReLU를 쓰지 않는 이유
    - ReLU는 양수 입력에 제약이 없음(0~∞)
    - RNN은 동일한 가중치를 매 시간 단계마다 반복해서 곱함
    - 값이 1보다 조금만 커도 시간 단계를 거치며 복리처럼 기하급수적으로 커짐 → 기울기 폭발(Exploding Gradient)
    - tanh는 출력을 -1~1로 강제로 묶어(Bounding) 이 폭발을 방지

##### 1.4 장단기 메모리 (LSTM)
 - (Long Short-Term Memory)
   - 단기기억(은닉상태)
   - 장기기억(셀상태)

---





## 찾아본 내용**