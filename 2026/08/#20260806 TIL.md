# 20260806 TIL

교재 진도 : python 
        - 1장 1-3강
        - 2장 1강

## 어제의 복습  
 1. pr 머지 전략 중 squash and merge의 특징으로 가장 적절한 것
 2. 작업이 미완성 상태일 때 변경 사항을 임시로 스택에 저장하고 워킹 트리를 마지막 커밋 상태로 되돌리는 Git 명령어
 3. 코드 리뷰에서 리뷰어가 request changes를 선택했을 때 발생하는 상황
 4. PR 본문에 'closes #12'와 같이 특정 키워드와 issue 번호를 함께 작성하면, PR이 머지될 때 자동으로 일어나는 동작  
 5. Github와 Git flow를 비교한 설명  

## 오늘의 학습

### 개발환경 
깃허브에서 리포시트리 ax_study 생성  
ax_study 폴더 생성   
전체 경로 복붙 cmd 에서 cd 로 이동  
git init  

github ssh 주소 복사  
git remote add origin git@github.com:Alexandros509/ax_study.git  

가상 환경 격리 필요성
각 프로그램마다 요구 버전이 다를 수 있는데 하나의 공간에 전역 설치하면 환경 출동로 다른 프로그램들도 망가짐  

구축 방법
1. python -m venv venv  
2. 아나콘다  
위 2 방법 느림. 의존성 문제 자주 발생  
3. uv  
 - 프로젝트 단위 관리 , 의존성 궁합 해결 , rust 기반 초고속  

 uv 설치  
  windows - powershell
 ```powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"```
 To add C:\Users\417no\.local\bin to your PATH, either restart your shell or run:

    set Path=C:\Users\417no\.local\bin;%Path%   (cmd)
    $env:Path = "C:\Users\417no\.local\bin;$env:Path"   (powershell)

설치 여부 및 버전 확인  
`uv --version`  

**버전 표시 방식** 
version A.B.C  
    - A : major ver ( 하위 버전과 호환 X )
    - B : minor ver ( 호환 O , 기능 추가 )
    - C : patch ver ( 오류 수정 , 코드 개선 )  

uv 프로젝트 생성 + 가상환경 구축
 `uv init --python 3.13`

폴더에 `uv init`
`pyproject.toml`으로 프로젝트 확인 가능

표 관련 도구  
`uv add pandas`

의존성의 의존성  
pandas를 설치하는 다른 도구도 5개가 설치됨  

설치 후 `pyproject.toml` dependencies에서 `pandas` 확인 가능

`.python-version` 활성화
 명령어 venv > Scripts > activate.bat
 `.venv\Scripts\activate`  
 - 실행 중 오류 발생. vs code terminal에서 명령 중 보안 관련 오류. cmd에서 실행 후 정상 작동. 
 비활성화 `.venv\Scripts\deactivate.bat`

cd ..
ax_study>`uv init ex_venv --python 3.1`

**가상환경 구축**
 1. python 실행
 2. 의존성 추가
 3. sync 명령어 

`uv sync`는 윈도우나 터미널에게 "내 가상환경을 uv.lock 파일에 적힌 레시피와 토씨 하나 안 틀리고 똑같이 맞춰 놔!"라고 지시하는 명령어  

다른 가상환경에서 실행하고 다운해도 다른 환경과는 분리  

`uv python pin 3.12`  
 - `pyproject.toml` 명세서에서 python 요구 버전 낮춰 수정 후 버전 변경 및 명시

가상환경 입장 후 버전 확인 > 3.13.14 
uv python pin 3.12 > uv sync 
 sync 과정 중 오류 pandas 관련 의존성으로 메세지 출력됨
but python --version은 3.12.13으로 변경되어있음

02_PYTHON 폴더에 main.py 생성  
uv run main.py > 위의 pandas 관련 오류 > uv python pin 3.13 > uv sync > uv remove pandas  

 uv python pin 3.13 > uv sync  
 설계도(.python-version) 변경 > 테라포밍 실행  

문법 오류 SyntaxError: invalid syntax  

가상환경 활성화된 창에 python 입력 >
```
>>>1+2
3
>>>exit()
```

extension
 python, jupyter(python에 맞는 engine 설치)

`uv add ipykernel`
 - Jupyter Notebook 실행 환경을 연결하기 위한 핵심 패키지인 ipykernel을 추가하고 설치하는 명령어

 03.ipynb
 - select kernel > python environment > 가상환경의 python (권장-별표)  
  동작 여부 확인차 code 클릭  
    shift + enter : 실행 및 새 code 칸 생성  
    ctrl + enter : 실행  
    a (above) : 위에 새 code 생성  
    b (below) : 아래에 새 code 생성  
    m : 코드 > 마크다운
    y : 마크다운 > 코드
    c : copy
    v : paste
    x : cut
    dd: delete
    z : cancel

ax_study ni .gitignore  
 .venv/ > 소스트리 실행 ax_study 폴더 추가  모두 스테이지 올리기 > 커밋 메시지 추가 > 즉시 푸시 활성화 > 커밋
 
주피터 셀에서 파이썬 실행이 순차적이 아닐수도 있다. > restart
 clear all outputs : 결과값, 실행횟수 초기
 예시) 
 ```
 money = 5000
 ```
 ```  
 money = money + 2000  
 print(money)
 ```    

### python 문법

  변수와 동적 타이핑  
   1. 변수?    
        money = 5000
        > 5000 값 주소 생성 , money 주소 이름표/키     
     예시) 
     ```
     x=5000
     x
     ```
     id(x)=1781782405360
     ```
     y=x
     y
     ```
     id(y) = id(x)
     >>**참조(Reference)**
     변수 : 값을 가진 공간의 주소를 가진 이름표
        변수명 = 값  ( =가 대입연산자 )

     참조 끊김 > garbage collector ( 청소부 )  
     
    **변수명 명명 규칙**  
    1. 영문 (대소문자) , 숫자 , $ , _ 로 구성
    2. 숫자는 앞에 X
    3. 공백 포함 X
    4. 핵심 키워드 사용 X ( if, for, true ... )

    **변수명 권장 사항**
    1. 의미있는 단어
    2. 여러 단어 구성은 _로 구분

    자료
        > 연산에 사용되는 값
    
    자료형 : 자료의 종류
    1. 숫자형
    2. 문자형 (' ') , ("")
    3. 논리형 : True , False , _bool

  동적 타이핑
  ```
pi = 3.14
type(pi)
>float
 ```
 ```
 result =True
type(result)
>bool
 ```
 ```
 pi="원주율"
type(pi)
>str
 ```




## 과제 중 풀었지만 이해가 안 가는 심화문제
### 1. 1장 1강 심화 1
 심화 1 : 파이썬 프로그램 진입점 함수 구현


자동으로 생성된 main.py 파일의 구조를 참고하여 진입점 함수를 직접 작성하고 호출해 봅니다.   


#### 문제 3-1 : main 함수 구조 완성하기


**요구사항**

---

- def 키워드를 사용하여 main이라는 이름의 함수를 정의하고 내부에 출력 코드를 작성하십시오.
- if name == "__main__": 구조를 활용하여 정의한 함수가 자동으로 실행되도록 코드를 작성하십시오.

아래와 같이 작성했지만 터미널에서 오류 뜨고 안내 메세지에 따라 조금씩
수정하면서 완료했을 뿐 제대로 된 것 같지 않음.
실제로 -> None에서 살짝 이해가 안 되서 그 부분을 지움.
반환값이라던데 잘 모르겠음.
```
def main() :
    main = print("Hello~")

if __name__ == "__main__" :
   main

main()
```

4번 문항
REPL(대화형 셸) 방식과 스크립트(.py) 구동 방식을 비교한 설명으로 옳지 않은 것은 무엇인가요?

B. 스크립트 방식은 파일에 코드를 저장하므로 영구적으로 보존된다

오답이에요
REPL은 휘발성을 가져 터미널이나 세션을 종료하면 작성했던 코드가 메모리에서 모두 사라집니다. 반대로 스크립트 방식은 파일로 저장되어 영속성을 갖습니다.




### 찾아본 내용
 - uv 설치 중 파워셸에서  To add C:\Users\417no\.local\bin to your PATH와 같은 안내 봄. bin을 add하는 목적은?
   내 컴퓨터의 어느 폴더 위치에서든 uv 명령어를 곧바로 실행하기 위해서  
     PATH에 추가하지 않았을 때:
     파워셸에서 uv를 실행하려면 매번 파일이 있는 전체 경로를 다 적어줘야 합니다.

      `C:\Users\417no\.local\bin\uv init`

     PATH에 추가했을 때:
     윈도우가 명령어 위치를 이미 알고 있기 때문에, 이름만 쳐도 알아서 찾아 실행해 줍니다.

      `uv init`


 - `.venv\Scripts\activate `를 vs code terminal에서 실행하고 오류가 떴는데 cmd에서 실행하니까 되는 차이?
     VS Code 터미널 (기본값: PowerShell): 파워셸(PowerShell)은 엄격한 보안 정책이 적용되어 스크립트 실행이 차단되고 오류가 발생
     CMD (명령 프롬프트): 보안 제약이 없다. 명령어가 차단 없이 정상적으로 작동
      VS Code에서도 계속 CMD를 쓰고 싶다면, 터미널 창 우측 상단의 + 버튼 옆에 있는 아래쪽 화살표(∨)를 누른 뒤 [Command Prompt]를 선택하면 된다.

 - vs code terminal에서 txt 파일 실행 
   `code name.extension`