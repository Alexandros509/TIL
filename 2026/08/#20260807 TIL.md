# 20260807 TIL
# 오늘의 TIL

> 날짜 : 2026-08-07
> 주제 : 파이썬 기초 (기본 자료형, 형변환, 연산자, 문자열 형식화)

---

## 어제의 복습

1. `uv init`, `.python-version`, `pyproject.toml`
2. `uv init` - Jupyter Notebook `.venv` - `uv add ipykernel`
3. REPL과 `.py` 스크립트 파일의 차이점
4. `id()` 함수
5. 변수의 개념: 값을 담는 상자가 아니라 '나는 이거야'라고 대상을 가리키는 **참조(이름표)**

---

## 오늘 공부한 내용

### 2.2 기본 자료형과 형변환

#### 2.2.0 주석 (Comment)
- 코드 상에 설명을 추가할 때 사용합니다.
- 인터프리터가 해석하지 않으므로, 코드 실행을 배제할 때도 사용합니다.
- 한 줄 주석: `#`
- 여러 줄 주석: `"""..."""`

```python
# 코드 설명...
"""
한줄
두줄
세줄
"""
```

#### 2.2.1 Python의 기본 자료형 (Primitive Data Types)
- **자료**: 연산에 필요한 값(데이터)
- **자료형**: 값의 유형(데이터의 종류)

1. **수치형**: 정수(`int`)와 실수(`float`)
2. **문자형**: 문자열(`str`)
   - 작은따옴표(`''`)나 큰따옴표(`""`)로 감싸서 선언 (한 줄)
   - `"""..."""`는 여러 줄 문장에 사용
3. **논리형**: 불리언(`bool`) - 참(`True`)과 거짓(`False`)
   - 참(`True`): 0이 아닌 숫자, 글자가 포함된 문자열, 요소가 채워진 리스트
   - 거짓(`False`): 0, 빈 문자열 `""`, 빈 리스트 `[]`, 데이터 없음 `None`
4. **값 없음**: `None`

#### 2.2.2 명시적 형변환 (Explicit Type Conversion)

> **외부 데이터를 문자열 형태로 입고받아, 개발자가 직접 타입을 변경하는 작업 (마치 포장을 바꾸는 것과 같음)**

- 연산은 같은 자료형끼리만 가능하므로 형변환이 필요합니다.

| 함수 | 기능 |
|---|---|
| `int()` | 정수로 변환 |
| `float()` | 실수로 변환 |
| `bool()` | `True`, `False`로 변환 |
| `str()` | 문자형으로 변환 |

```python
num = "100"
type(num) # > str

# num + 200 
# > TypeError 발생

int(num) + 200 
# > 300

# "현재 시간은" + 10 + "시" 
# > TypeError 발생

"현재 시간은" + str(10) + "시" 
# > '현재 시간은10시'
```

#### 2.2.3 묵시적 형변환 (Implicit Type Conversion)
- 파이썬 내부에서 자동으로 타입을 변환하는 기능입니다.
- 정수와 실수를 연산할 때 발생하며, 데이터 손실을 막기 위해 더 넓은 범위인 `float`으로 업캐스팅(Upcasting) 됩니다.

```python
num1 = 100
num2 = 3.123
type(num1), type(num2) 
# > (int, float)

num3 = num1 + num2 # num1이 int -> float 자동 형변환
num3, type(num3)
# > (103.123, float)

# 강제로 명시적 형변환 적용 시
num3 = num1 + int(num2)
num3, type(num3)
# > (103, int)
```

#### 2.2.4 형변환 시 주의해야 할 예외와 에러 (ValueError)
- 순수 숫자가 아닌 문자가 섞인 문자열을 형변환하려고 하면 실패하며 에러가 발생합니다.
- 안전한 형변환을 위해 사전에 `.isdigit()` 메서드로 검증하는 것이 좋습니다.

```python
time = "10시"
# int(time) + 1 
# > ValueError 발생

time = "10시"
if time.isdigit():
    int(time) + 1
```

---

### 2.3 연산자

#### 2.3.1 항과 연산자
- **항**: 연산에 사용되는 값이나 변수
- **연산자**: 연산에 사용되는 기호

**1. 사칙연산 및 특수 산술 연산자**
- 대입 연산자(`=`): 연산 우선순위 최하위. 가장 마지막에 대입을 수행합니다.
- 사칙연산: `+`, `-`, `*`, `/`
- 몫 연산자(`//`): 나눗셈 후 소수점을 버리고 몫만 추출하여 반환 (예: `10 // 3 = 3`)
- 나머지 연산자(`%`): 나눗셈 후 나머지만 반환 (예: `10 % 3 = 1`)
- 거듭제곱 연산자(`**`): 제곱 연산 수행 (예: `2 ** 3 = 8`)
- **연산 우선순위**: `**` > `*`, `/`, `//`, `%` > `+`, `-`
  - 소괄호 `()`를 사용하여 우선순위를 강제로 지정할 수 있습니다.

**2. 복합 대입 연산자**
- 코드를 단축하여 현재 변수값에 연산을 수행한 뒤 그 결과를 즉시 갱신합니다.
- 예: `x = x + 10` ➔ `x += 10`
- 종류: `+=`, `-=`, `*=`, `/=`, `//=`, `%=`, `**=`

**3. 비교 연산자**
- 판별이 필요한 구문(조건문, 반복문 등)에 사용하며, 결과는 무조건 `True` 또는 `False`입니다.
- 문자열을 비교할 때는 알파벳순(사전 순)으로 정렬하여 크기를 식별합니다.
- 비교 기호: `>`, `<`, `>=`, `<=`, `==`(같다), `!=`(다르다)

| 비교 기준 | 기호 | 설명 |
|---|---|---|
| **동등성** | `==` | 동일한 가치(값)를 가지고 있는지 비교 |
| **동일성** | `is` | 메모리상 동일한 대상(주소)인지 비교 |

```python
num1 = 1000
num2 = 1000

id(num1), id(num2)
# > (2394777249744, 2394777249584)   # 주소가 다름(diff)

num1 == num2 # 동등성 비교
# > True

num1 is num2 # 동일성 비교 (주소 비교)
# > False
```

**4. 논리 연산자**
여러 개의 조건을 묶어서 하나의 최종 판단을 내리는 조건 결합기입니다.

| 연산자 | 작동 원리 | 비고 |
|---|---|---|
| `and` | 각 항이 모두 `True`이면 `True`, 하나라도 `False`면 `False` | 논리곱 |
| `or` | 각 항 중 하나라도 `True`이면 `True`, 모두 `False`면 `False` | 논리합 |
| `not` | `True` 조건 ➔ `False`, `False` 조건 ➔ `True`로 반전 | 논리부정 |

**5. 부호 반전 연산자**
- 숫자의 부호를 반대로 변경하는 단항 연산자입니다.

**6. 단락 회로 평가 (Short-Circuit Evaluation)**
- 논리 연산의 결과가 왼쪽 조건만으로 확정되면, 이후 조건은 더 이상 검사하지 않고 평가를 종료하는 최적화 방식입니다.

```python
result = True or 10**2
result
# > True (오른쪽 연산 무시)

result = False or 10**2
result
# > 100 (오른쪽 연산 수행)
```

**7. 연산자 우선순위**
> 대입 연산자 < 논리 연산자 < 비교 연산자 < 산술 연산자

---

### 2.4 문자열 형식화

#### 2.4.1 문자열 형식화 3가지 기법

**1. 1세대: `%` 연산자 방식**
- `%` 연산자를 사용해 뼈대 문자열에 값을 삽입하는 방식입니다.
- `%s` (문자열): `%5s`는 5칸 공간 확보 후 우측 정렬. `%05s`는 남는 공백을 0으로 대체.
- `%d` (정수)
- `%f` (실수): `%.2f`는 소수점 둘째 자리까지 표기. `%A.Bf`는 전체 A칸 중 소수점 B자리까지 표기.

```python
print("기본 출력: %s, %d" % ("홍길동", 20))
print("너비 지정 (5칸 우측 정렬): [%5d]" % 123)
print("소수점 지정 (소수점 둘째 자리): %.2f" % 3.14159)
```

**2. 2세대: `str.format()` 메서드 방식**
- `{}`에 인덱스 번호나 이름을 지정해 값을 삽입합니다.
- 변수가 많아지면 괄호 내부가 복잡해진다는 단점이 있습니다.
- 교체 방식: 인덱스 지정(`{0}`, `{1}`), 이름 지정(`{name}`, `{age}`)

```python
message = "내 이름은 {0}이고, 나이는 {1}입니다"
message.format("철수", 20)
# > '내 이름은 철수이고, 나이는 20입니다'

message = "내 이름은 {name}, 나이는 {age}이다."
message.format(name="철수", age="20")
# > '내 이름은 철수, 나이는 20이다.'
```

**3. 3세대: f-string 방식**
- 문자열 따옴표 앞에 `f`를 붙여 변수나 표현식을 직접 삽입하는 방식입니다.
- **서식 지정**: 콜론(`:`) 뒤에 지시자를 사용하여 패딩이나 쉼표 등 출력 형식을 지정합니다.
- **디버깅 조절자(`=`)**: 변수명 뒤에 `=`를 붙이면 변수명과 값을 함께 출력하여 로그 확인 시 매우 유용합니다.

```python
name = "영희"
age = 20

message = f"내 이름은 {name}, 나이는 {age}이다."
# > '내 이름은 영희, 나이는 20이다.'

price = 10000
f"가격 : {price:,}"
# > '가격 : 10,000'

f"가격 : {price:>10,}원"
# > '가격 :      10,000원'
```

---

## 찾아본 내용

- **`.toml`**: `Tom's Obvious, Minimal Language`. GitHub 공동 창립자 톰이 만든 명백하고 최소한의 설정 파일 언어.
- **`.venv`**: `Virtual Environment`. 파이썬 가상환경의 줄임말.
- **`REPL`**: `Read-Eval-Print Loop`. 명령을 읽고 평가하고 출력한 뒤, 다시 입력을 기다리는 반복 대화형 프로그래밍 환경.

---

## 핵심 개념

- 변수의 참조 (포인터 개념)
- 명시적 형변환과 묵시적 형변환(Upcasting)
- 동등성(`==`)과 동일성(`is`) 비교의 차이점
- 논리 연산의 단락 회로 평가(Short-Circuit Evaluation)
- f-string 정렬 및 디버깅 조절자(`=`)

---

## 교재 보충

※ 아래 내용은 사용자의 필기를 보완하기 위해 교재에서 가져온 내용입니다.

- **NameError 주의**: 불리언(bool) 자료형을 사용할 때, 소문자로 `true`, `false`라고 적으면 파이썬은 이를 키워드가 아닌 정의되지 않은 변수명으로 인식하여 에러를 발생시킵니다. 반드시 대문자로 시작(`True`, `False`)해야 합니다.
- **`input()` 함수의 형변환**: 사용자에게 키보드로 값을 입력받는 함수는 숫자를 입력했더라도 무조건 '문자열'로 인식합니다. 따라서 연산에 투입하려면 반드시 `int()`나 `float()`로 묶어 명시적 형변환을 거쳐야 합니다.
- **`str.format()` 방식의 한계**: 2세대 방식은 대입할 변수의 개수가 많아질수록 앞쪽의 문자열 틀과 뒤쪽 괄호 안의 매개변수 사이의 거리가 멀어져 대형 데이터 처리 시 가독성이 떨어집니다. 
- **나머지 연산자(`%`)의 실무 활용**: 배수 검사나 홀수/짝수 판별 등 실무 알고리즘 조건문에서 매우 빈번하게 사용됩니다.

---

## 필기 ↔ 교재 비교

| 항목 | 필기 | 교재 | 결과 |
|------|------|------|------|
| 명시적 형변환 | 입고된 외부 데이터를 직접 타입 변경 (like 포장) | 개발자가 의도를 가지고 직접 바꾸는 기술 | 필기 유지 |
| 묵시적 형변환 | float upcasting (자동 변환) | 데이터 손실 방지를 위한 자동 스케일 업 (업캐스팅) | 동일 |
| 비교 연산자 | `==`(동등성)과 `is`(동일성) 함께 다룸 | `==`, `!=`, 크기 비교 등만 다룸 | 필기 유지 |
| 불리언 대소문자 주의 | 내용 없음 | 소문자 사용 시 NameError 발생 경고 | 교재 보충 |

---

## 편집 로그

### 수정한 내용
- 맞춤법 수정 (수정한 문장 수: 1) - `ptproject.toml`을 올바른 파이썬 설정 파일 이름인 `pyproject.toml`로 교정
- 교재 보완 수: 4
- 문장 분리 및 구조 개선
- Markdown 표 생성

### 수정하지 않은 내용
- 경험 및 비유 ("외부 데이터 문자열 형태로 입고, 개발자가 직접 타입 변경 like 포장")
- 코드 및 에러 발생 실습 내용
- 교재 범위를 넘는 동일성 검사 실습 (`is` 연산자, `id()` 함수)

### 추가 정보 출처
- 필기
- 교재
  
# 원본 필기 (Rough Notes)

## 어제의 복습
 1. uv init, .python-version , ptproject.toml
 2. uv init - jupyter notebook .venv - uv add ipykernel
 3. REPL <> .py diff
 4. id()

 변수? '나는 이거야' 가리키는
 참조 

## 오늘의 학습
   교재 : python 기초  
        - 2장 2-4강

 ### 2.2 기본 자료형과 형변환
 #### 2.2.0 주석 (Comment)
    - 코드 상에 설명을 추가할 때 사용
    - 인터프리터가 해석 X > 코드 실행 배제에도 사용
    - # : 한줄 주석

    
    ex) 
    # 코드 설명...
    """
    한줄
    두줄
    세줄
    """

 #### 2.2.1 Python의 기본 자료형 (Primitive Data Types)
  - 자료 : 연산에 필요한 값(데이터)
  - 자료형 : 값의 유형(데이터의 종류)
   1) **수치형** : 정수(int) 와 실수(float) 
   2) **문자형** : 문자열(str)
      - '',"" 감싸서 선언(한줄), """...""" 여러 줄 문장
   3) **논리형** : 불리언(bool) - 참(True) & 거짓(False)
     - 참(True) : 0 아닌 숫자, 글자 포함 문자열, 채워진 리스트
     - 거짓(False) : 0, 빈 문자열 "", 빈 리스트 [], 데이터 없음 None
   4) **값 없음** : None  

#### 2.2.2 명시적 형변환 (Explicit Type Conversion)
  - **외부 데이터 문자열 형태로 입고, 개발자가 직접 타입 변경 like 포장**
  - 연산은 같은 자료형끼리 가능
  - 형변환 함수
   1. `int()` : 정수로 변환
   2. `float()` : 실수로 변환
   3. `bool()` : True,False로 변환
   4. `str()` : 문자형으로 변환
```
num ="100"
type(num) > str

num+200 
> TypeError
-----------
num ="100"
type(num) > str

int(num)+200 > 300
-----------
"현재 시간은" + 10 + "시" 
> TypeError
"현재 시간은" + str(10) + "시" 
> '현재 시간은10시'
```
#### 2.2.3 묵시적 형변환 (Implicit Type Conversion)
 - 내부에서 자동 변환
   1. 정수+-x/실수 
   2. float upcasting
```
num1=100
num2=3.123
type(num1), type(num2) 

> (int,float)

num3=num1+num2 # num1이 int -> float 자동 형변환(묵시적 형변환)
num3 , type(num3)

> (103.123, float)

#num3=num1+num2
num3=num1+int(num2)
num3 , type(num3)

> (103, int)
```
#### 2.2.4 형변환 시 주의해야 할 예외와 에러 (ValueError)
   -  문자열 형변환 실패 
   -  안전한 형변환을 위한 사전 검증 함수 `.isdigit()` 메서드

```
time = "10시"
int(time)+1

>ValueError

time = "10시"
if time.isdigit():
    int(time)+1
```
### 2.3 연산자
#### 2.3.1 항과 연산자
  - 항 : 연산에 사용되는 값, 변수  
  - 연산자 : 연산에 사용되는 기호  
   1. 사칙연산 및 특수 산술 연산자  
    0. 대입 연산자 : `=` 연산 우선 순위 최하위. 가장 마지막에 연산  
    1. 사칙연산 : `+` , `-` , `*` , `/`  
    2. 몫 연산자 : `//` 몫만 추출, 반환  ex) 10//3 = 3  
    3. 나머지 연산자 : `%` 나머지만 반환  ex) 10%3 = 1  
    4. 거듭제곱 연산자 : `**` 제곱 ex) 2**3 = 8(2^3)  
    - 연산 우선순위: `**` , `*` , `/` , `//` , `%` > `+` , `-`   
    - 우선 순위 강제 적용 : ()
  
   2. 복합 대입 연산자  
    - 코드 단축  
      x= x+10 -> x+=10  
    - 주요 연산자 종류   
      `+=` , `-=` , `*=` , `/=` , `//=` , `%=` , `**=`

   3. 비교 연산자  
    - 연산의 결과는 무조건 True, False  
    - 판별이 필요한 구문 (조건문, 반복문)  
    - 문자열 비교 식별은 알파벳순 정렬  
      `>` , `<` , `>=` , `<=`  
      `==` : 같다  
      `!=` : 다르다

      **동등성 vs 동일성**  
      - 동등성 : 동일한 가치를 비교(==)
      - 동일성 : 동일한 대상인지 비교 (is - 주소 비교)
        ```
        num1=1000
        num2=1000

        id(num1), id(num2)
        > (2394777249744, 2394777249584)   # diff!

        num1==num2 #동등성 비교
        >True

        num1 is num2 #동일성 비교(주소 비교)
        >False
        ```

   4. 논리 연산자  
    - 여러 개의 조건을 연산자로 묶어서 하나의 최종 판단을 내리는 조건 결합기  
     1. `and` : 각 항이 모두 True -> True, 하나라도 False -> False - `논리곱`  
     2. `or` : 각 항의 하나라도 True -> True, 모두 False -> False - `논리합`  
     3. `not` : True 조건 -> False 조건으로 반전, False 조건 -> True 조건으로 반전 - `논리부정`  
   
   5. 부호 반전 연산자  
    - 숫자의 부호를 반대로 변경하는 단항 연산자  
   
   6. 단락 회로 평가 (Shor-Circuit Evaluation)  
    - 논리 연산의 결과가 확정되면 이후 조건을 검사하지 않는 평가 방식

    ```
    result = True or 10**2
    result
    >True

    result=False or 10**2
    result
    >100
    ```
   7. 연산자 우선순위  
        대입 연산자 < 논리 연산자 < 비교 연산자 < 산술 연산자

### 2.4 문자열 형식화
#### 2.4.1 문자열 형식화 3가지 기법
 1. 1세대 : %연산자 방식  
  - % 연산자를 사용해 값을 문자열에 삽입하는 방식  
    1. `%s` : 문자열  
    - %숫자s -> 숫자 자리수 만큼 공간 확보, 우측 정렬 , 남는 공간 공백  
    - %대체문자s -> %05s -> 남는 공백 0으로 대체  
    2. `%d` : 정수  
    3. `%f` : 실수  
     - %.숫자f -> 소수점 숫자까지만큼 소수점 표기 
     - %A.Bf -> 정수부 A , 소수점 B까지 표기
     ```
     print("기본 출력: %s, %d" % ("홍길동", 20))
     print("너비 지정 (5칸 우측 정렬): [%5d]" % 123)
     print("소수점 지정 (소수점 둘째 자리): %.2f" % 3.14159)
     ```
 2. 2세대 : str.format() 메서드 방식
  - {}에 인덱스나 이름을 지정해 값을 삽입, % 방식보다 유연하고 사용이 편리하다
  - 변수 많아지면 복잡  
  - 순서 지정 교체 : {0} {1} {2}  
    단어 지정 교체 : {단어} {단어}
    ```
    message = "내 이름은 {0}이고, 나이는 {1}입니다"
    message.format("철수",20)
    > '내 이름은 철수이고, 나이는 20입니다'

        message = "내 이름은 {name}, 나이는 {age}이다."
    message.format(name="철수",age="20")
    > '내 이름은 철수, 나이는 20이다.'

        message = "내 이름은 {name}, 나이는 {0}이다."
    message.format(20, name="철수")
    '내 이름은 철수, 나이는 20이다.'
    ```

 3. 3세대 : f-string 방식
  - 문자열 앞에 f를 붙여 변수나 표현식을 직접 삽입하는 방식
  - 서식 지정 : 콜론(:) 뒤에 지시자를 사용해 출력 형식을 지정하는 기능
  - 디버깅 조절자(=): 변수명 뒤에 =를 붙여 변수명과 값을 함께 출력, 로그 확인과 디버깅에 유용
    ```
    name="영희"
    age=20

    message = f"내 이름은 {name}, 나이는 {age}이다."
    message
    > '내 이름은 영희, 나이는 20이다.'

    price=10000
    f"가격 : {price:,}"
    > '가격 : 10,000'

    price=10000
    f"가격 : {price:>10,}원"
    > '가격 :     10,000원'
    ```

### 찾아본 내용
 - `.toml` : `Tom's Obvious, Minimal Language` , 톰(GitHub 공동 창립자)의 명백하고 최소한의 언어. 설정 파일
 - `.venv` : `Virtual Environment` 가상환경 줄임말
 - `REPL` : `Read-Eval-Print Loop` , 읽고 평가하고 출력한 뒤 다시 입력을 기다리는 반복 대화형 프로그래밍 환경